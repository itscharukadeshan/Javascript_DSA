/** @format */

const getSum = (A, B) => {
  const firstNumber = A;
  const secondNumber = B;

  const sumOfNumbers = firstNumber + secondNumber;

  return sumOfNumbers;
};

const result = getSum(40, 2);

console.log(result);
